# Kyd SaaS Starter Acceptance Matrix

> Task: `STS-030`  
> Authority ID: `KSS-DS-ACCEPT-R1`  
> Version: `R1`  
> Status: `FROZEN`  
> Authoring Authority: `KSS-DS-AUTH-R1`  
> Correction: `KSS-STS022-CORRECTION-R1`; `KSS-STS021A-RESOLUTION-R2`; `KSS-STS024-RESIDUAL-CORRECTION-R1`
> Freeze Authority: `KSS-STS030-FREEZE-R1`  
> Freeze artifact: `KSS-DS-FREEZE-R1`  
> Independent freeze prerequisite: `KSS-STS025-REAUDIT-RESULT-R2` (`PASS`)

This frozen matrix defines future observable acceptance. Design freeze does not execute application, provider, Cloudflare, or simulated-production checks. `CONDITIONAL_IF_ENABLED` applies to enabled optional or retained-disabled capabilities; their OFF-path acceptance remains mandatory.

| Acceptance ID | Capability ID | Requirement | Verification type | Required environment | Gate | Evidence expected |
|---|---|---|---|---|---|---|
| ACC-001 | CAP-001 Framework | App Router public/Auth/account/admin/member routes, middleware, route handlers, and representative Server Actions build and return their expected status/render state through the selected adapter. | Build + integration smoke | CI and STAGING | Static/build; Cloudflare runtime | Clean build log plus route/action smoke results for the recorded commit. |
| ACC-002 | CAP-002 Cloudflare/OpenNext | The recorded commit deploys through in-tree OpenNext/Wrangler commands to isolated Cloudflare DEVELOPMENT and STAGING; a core route and health revision are reachable. | Deployment integration | DEVELOPMENT and STAGING | Cloudflare deploy | Deployment IDs, Worker revision, command result, and HTTP smoke evidence. |
| ACC-003 | CAP-003 Environment/config | Every section 21 row enforces owner/class/required-if/default/environment rules. Replicate OFF needs no credential or initialization; ON validates one logical server credential at the existing provider-construction/selection boundary without freezing an unsupported physical name. Every public value is `ARTIFACT_BOUND` or `ENVIRONMENT_RUNTIME_BOUND`; the promoted artifact fingerprint is unchanged, target runtime values resolve correctly, and no STAGING value or secret leaks to PRODUCTION. | Matrix unit + startup negative + provider injection + artifact/runtime scan | CI, DEVELOPMENT, STAGING, PRODUCTION configuration review | Config/security | Row-keyed results, Replicate OFF/ON provider call counts, safe-example/client-secret scan, artifact public-config fingerprint equality, and redacted target runtime-public binding/DOM trace. |
| ACC-004 | CAP-004 PostgreSQL/Drizzle | Selected PostgreSQL environment accepts existing Drizzle CRUD and a real transaction through generic `DATABASE_URL` without provider SDK in domain code. | Database integration | Isolated DEVELOPMENT and STAGING | Provider Qualification; DB smoke | Migration/CRUD/transaction results, dependency/import scan, and selected provider qualification record. |
| ACC-005 | CAP-005 Migrations | Baseline migrations succeed on a fresh isolated DB, repeated migrate is non-destructive, and schema drift check matches the committed schema/migration history. | Migration integration | CI disposable DB and STAGING preflight | Migration/release | Fresh/repeat command results and schema drift evidence. |
| ACC-006 | CAP-006 DB atomicity | Credits OFF creates a canonical user with no credit; credits ON commits one user plus exactly one initial grant or neither and concurrent trusted-email creation yields one user. Affiliate OFF exposes no mutation route; ON commits invited-by plus one affiliate relation or neither, while invalid/self/repeated/wrong-owner attempts change nothing. Payment uses ACC-016. | Transaction + concurrency + authorization integration | CI disposable DB | DB/security | Before/after row counts for each named OFF/ON, injected-failure, concurrent, and invalid invite case. |
| ACC-007 | CAP-007 Auth core/session | Successful Auth establishes the canonical UUID payload while retaining pinned Auth.js lifetime/update/cookie defaults. Implementation records/tests those defaults without invented numeric policy. A custom product lifetime/update/cookie override passes only when Product-Specific Design explicitly owns, documents, and tests it; absent that Authority, the Starter baseline remains unchanged. | Auth integration + browser + resolved-default/ownership record | CI and STAGING | Auth/security | Pinned package/config record, resolved property assertions, override-authority positive/negative check, denial responses, logout trace, and STAGING Auth smoke. |
| ACC-008 | CAP-008 Google OAuth | Verified Google identity creates/uses one canonical UUID; unverified or invalid provider identity is rejected before persistence/linking. | Provider/Auth integration | Provider test configuration in STAGING | Auth identity security | Redacted callback cases, DB identity assertion, and rejection evidence. |
| ACC-009 | CAP-009 Email Magic Link | Initial request creates one active credential valid 15 minutes. An accepted re-request before expiry resends that same logical link, creates no second credential, and sets expiry to latest request +15 minutes; the delayed first email remains usable. Successful use consumes atomically; replay and expiry reject; the first accepted post-expiry request creates a new credential. Throttling is tested separately, and Google/Magic-Link orders/concurrency yield one UUID. | Auth + DB + controlled-time delivery integration | CI with delivery stub/clock; STAGING provider smoke | Magic Link security | Token identity/count/expiry timestamps, delayed-email trace, one-use/replay/expired/post-expiry results, independent throttle cases, and one-UUID DB evidence. |
| ACC-010 | CAP-010 Email/Resend | Auth uses one server-only adapter with server-generated content; absent config and provider rejection return controlled errors without logging token/key/full link. | Unit + integration | CI; STAGING when Magic Link enabled | Email/security | Adapter invocation assertions, template snapshot, error result, and redaction scan. |
| ACC-011 | CAP-011 Account/profile/logout | Authenticated owner can read/update only allowed profile fields; wrong owner/anonymous input fails; UI shows validation/error/loading states; logout blocks account access. | API integration + browser | CI and STAGING | Auth/account | Positive/negative API results and browser route-state trace. |
| ACC-012 | CAP-012 Orders | Server catalog controls product/amount/currency; valid checkout creates one non-terminal `created` order. Only verified facts apply `created -> paid/failed/cancelled` and `paid -> refunded`; `failed`, `cancelled`, and `refunded` are terminal. Verified duplicate/stale/incompatible facts cause no mutation and provider-appropriate success acknowledgement, with reconciliation evidence for stale/incompatible facts. Invalid/untrusted facts reject; redirects cannot mutate. | Domain state-table + route-outcome integration | CI; provider test mode in STAGING | Payment lifecycle | Every legal/illegal transition, duplicate/stale acknowledgement, reconciliation record, invalid/redirect rejection, owner authorization, and order-history assertion. |
| ACC-013 | CAP-013 Stripe one-time | Stripe callback/webhook accepts only server-verified/signed evidence. Invalid/untrusted evidence rejects before mutation; verified duplicate/stale evidence returns Stripe-route success acknowledgement without mutation; a valid transition failing before commit leaves idempotency incomplete and reports a retryable route failure; retry converges once through the retained shared path. | Provider + DB + route-outcome integration | CI; STAGING test mode | Payment/security | Redacted IDs, signature/retrieval cases, exact acknowledgement classes, rollback/idempotency counts, shared-path calls, side-effect counts, and retry result. |
| ACC-014 | CAP-014 Creem one-time | Creem callback/webhook accepts only server-verified/signed evidence. Invalid/untrusted evidence rejects before mutation; verified duplicate/stale evidence returns Creem-route success acknowledgement without mutation; a valid transition failing before commit leaves idempotency incomplete and reports a retryable route failure; retry converges once through the retained shared path. | Provider + DB + route-outcome integration | CI; STAGING test mode | Payment/security | Redacted IDs, signature/retrieval cases, exact acknowledgement classes, rollback/idempotency counts, shared-path calls, side-effect counts, and retry result. |
| ACC-015 | CAP-015 Payment provider switch | `stripe` selects only Stripe and `creem` only Creem; unsupported value fails configuration; request payload cannot override selection; no provider is invoked as automatic fallback. | Unit + API integration | CI | Payment config/security | Provider client mocks/call counts and invalid-config startup or checkout result. |
| ACC-016 | CAP-016 Webhooks/idempotency | Sequential/concurrent duplicates use provider event ID, or callback session ID plus normalized outcome, as one durable identity. Claim, legal state change, Core entitlement, and enabled effects commit once. Verified duplicate/already-applied and stale/incompatible facts are success-acknowledged no-ops; invalid/untrusted facts reject. Injected pre-commit failure commits neither business state nor completed idempotency and reports retryable failure; retry converges. | DB transaction/concurrency + provider route-outcome integration | CI disposable DB; STAGING smoke | Webhook idempotency security | Idempotency/order/entitlement/effect counts, duplicate/stale acknowledgement and anomaly evidence, invalid rejection, rollback with incomplete claim, and convergent callback/webhook retry output. |
| ACC-017 | CAP-017 Entitlement | `paid` qualifying order grants owner-queryable Core access in the payment transaction; `created`, `failed`, `cancelled`, `refunded`, revoked, and unknown deny; refund atomically revokes; caller can query only authorized ownership. | Domain + transaction + authorization integration | CI | Entitlement/payment | Full state-policy assertions, refund rollback/failure case, replay result, and negative authorization responses. |
| ACC-018 | CAP-018 AI providers | OFF mode exposes no callable AI route, requires no provider credential, and initializes no SDK. ON permits only authenticated callers and operator-allowed provider/model. Replicate selection validates/injects its logical server credential at the existing construction call before invocation; missing credential and disallowed/oversized input fail safely. | Config injection + API/security + conditional provider smoke | CI; STAGING if enabled | Conditional AI security | OFF route/config/provider-call scan, Replicate missing/valid credential injection call counts, policy negatives, limit result, redacted failure, and selected-provider smoke. |
| ACC-019 | CAP-019 i18n | OFF mode builds/routes without forced locale UX; ON mode resolves configured locales/fallbacks and emits correct localized canonical metadata. | Dual-mode build + browser/SEO integration | CI; STAGING if enabled | Optional-module/SEO | Two build results, route/canonical assertions, and fallback case. |
| ACC-020 | CAP-020 Ads/analytics | OFF config emits no provider script/request; each explicitly enabled provider initializes once and emits the expected pageview only after the configured consent condition. | Browser network/DOM smoke | CI browser; STAGING if enabled | Conditional browser/privacy | Network request trace, script count, pageview assertion, and consent state. |
| ACC-021 | CAP-021 Non-default Auth | Default build exposes neither GitHub nor One Tap entry; when one is enabled its credential/claim/trust and session tests pass before release. | Default-off scan + conditional Auth integration | CI; STAGING if enabled | Conditional Auth/security | UI/provider registration scan and enabled-provider test report. |
| ACC-022 | CAP-022 Storage/R2 | OFF mode requires no storage secret/route reachability; ON mode rejects unauthorized, oversized, disallowed-type and invalid-key requests and completes valid private upload/read in selected runtime. | API/security + conditional provider/runtime smoke | CI; STAGING if enabled | Conditional storage security | Negative response matrix, object metadata/privacy assertion, and Worker/provider smoke. |
| ACC-023 | CAP-023 Turnstile | OFF mode adds no widget/request/secret; ON named route accepts a valid token and rejects missing, invalid, expired, or replayed token according to selected policy. | Component + API + conditional provider smoke | CI; STAGING if enabled | Conditional abuse security | DOM/network OFF trace and server verification case results. |
| ACC-024 | CAP-024 UI Foundation | Every required Starter/Product reference item declares applicable `EXACT_SEMANTIC`, `TOKEN`, `STRUCTURAL`, `RESPONSIVE_STATE`, `ASSET_IDENTITY`, `EXPLICIT_TOLERANCE`, or `APPROVED_DEVIATION` rules. Tolerances are predeclared; deviations carry ID/item/owner/reason/scope/approval evidence. Undeclared differences create findings and failed required rules without approved deviation block. Two fresh non-author reviewers applying the same manifest reach the same gate result. | Component + browser responsive/a11y + repeat-review Fidelity Audit | CI browser and STAGING smoke | UI/a11y/fidelity | Manifest/version/rules, predeclared measurements, deviations, screenshots/interactions/a11y evidence, both reviewer identities/findings, and matching PASS/BLOCK result. |
| ACC-025 | CAP-025 Technical SEO | Configured origin drives metadata/canonicals; generated sitemap includes indexable routes; robots/noindex blocks STAGING and permits configured PRODUCTION routes; social metadata exists on Core public routes; required favicon/icon metadata/assets resolve; route-applicable structured data exists where stable route-owned facts warrant it, parses successfully, and matches visible/owned facts. | Metadata/structured-data unit + HTTP asset/crawl smoke | CI, STAGING, PRODUCTION preflight | SEO/release | Parsed metadata/canonical/structured-data assertions, icon asset responses, route-fact comparisons, sitemap/robots output, and environment indexability result. |
| ACC-026 | CAP-026 Optional content/UI | Default Starter has no visible route/menu requirement for blog/docs/charts/editor/carousel; each selected surface passes its focused render/navigation/security test. | Route/UI default-off scan + conditional browser | CI; STAGING if enabled | Conditional feature | Default route/menu inventory and selected-surface test report. |
| ACC-027 | CAP-027 Security controls | Every retained high-risk mutation/provider route has server-side identity/ownership and request validation; anonymous/wrong-owner/malformed/over-limit calls fail without state/provider side effect. | API negative/security suite | CI and STAGING smoke | Security | Route inventory mapped to guard/schema/limit plus response, DB, and provider call-count assertions. |
| ACC-028 | CAP-028 Logging/observability | High-risk failures emit structured event, severity, correlation ID and revision while redacting configured secret/token/PII patterns; health endpoint exposes readiness/revision without secret values. | Unit + deployed failure/health smoke | CI and STAGING | Logging/security/release | Captured log schema/redaction assertions and health HTTP/revision evidence. |
| ACC-029 | CAP-029 Testing/CI | Clean checkout CI runs Runtime validator, typecheck, lint, build, mandatory tests, secret/dependency scan, and conditional selected-capability tests; any failed mandatory gate stops the workflow. | CI self-verification | CI | Automated release | Workflow run and deliberate failing-gate proof or configuration assertion. |
| ACC-030 | CAP-030 Build quality | Explicit typecheck, lint and production build commands execute from pinned package/runtime inputs with no untracked generated application changes. | Static/build | CI | Static/build | Command logs, versions/lock integrity, and post-build Git status. |
| ACC-031 | CAP-031 Release/promotion | CI marks only a gated immutable STAGING commit/artifact plus artifact-bound public-config fingerprint eligible; operator approval references that bundle. PRODUCTION consumes the identical commit/artifact/fingerprint with no rebuild or mutation, resolves only approved target runtime-public bindings, leaks no STAGING public value or secret, and records migration/recovery readiness. | Release workflow + identity/fingerprint + runtime-config + approval verification | STAGING and PRODUCTION | Release/promotion | Commit/artifact/fingerprint equality, STAGING gate bundle, operator approval, no-rebuild assertion, redacted STAGING/PRODUCTION runtime-public maps and DOM/network no-leak evidence, migration/recovery record, and post-deploy revision smoke. |
| ACC-032 | CAP-032 Retained non-Core | Default Starter does not expose or require credits, subscriptions, API keys, affiliate, Vercel, or Docker; any advertised capability/deployment passes its owning conditional domain/security/build gate. | Default-off inventory + conditional domain/deploy smoke | CI; selected environment if enabled | Conditional feature/release | Default route/UI/config scan and selected feature/deployment evidence. |

## Coverage Check

- Material capabilities: 32.
- Acceptance records: 32.
- Core capabilities: each has at least one mandatory acceptance record.
- Optional and retained-disabled capabilities: each has mandatory OFF-path evidence and conditional ON-path evidence.
- Production/simulated-production checks executed during STS-010: none.
